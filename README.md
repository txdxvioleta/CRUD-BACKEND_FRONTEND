# CRUD-BACKEND_FRONTEND

#Levantar proyecto:
npm run dev (Vite)

#Levantar json-server:
npm run json-server || json-server --watch db.json (mover db.json a la raíz para que funcione con la segunda opción)

